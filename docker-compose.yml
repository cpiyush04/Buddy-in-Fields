version: '3.8'

services:
  farmer-assistant:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - QDRANT_ALLOW_RECOVERY_MODE=true
    restart: unless-stopped
